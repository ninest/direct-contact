{
  "name": "direct-contact",
  "version": "0.1.0",
  "license": "MIT",
  "scripts": {
    "workbox": "workbox",
    "clean": "rimraf ./public ./dist ./.cache ./.parcel-cache",
    "dev": "parcel index.html",
    "generate-sw": "workbox generateSW workbox-config.js",
    "build": "parcel build index.html && yarn generate-sw",
    "serve": "serve -s dist"
  },
  "devDependencies": {
    "prettier": "^2.2.1",
    "rimraf": "^3.0.2",
    "typescript": "^4.1.3"
  },
  "dependencies": {
    "autoprefixer": "^10.2.4",
    "parcel": "^2.0.0-nightly.574",
    "postcss": "^8.2.4",
    "serve": "^12.0.0",
    "tailwindcss": "^2.0.2",
    "workbox-cli": "^6.2.2"
  }
}